<div id="location-details">
	<ul>
		<% tidy_up(@location.attributes).each do |label, name| %>
			<li><%= label %>: <%= name %></li>
		<% end %>
	</ul>
</div>
<% if @location[:decimal_latitude] && @location[:decimal_longitude] %>
	<div id="map">
		<%= map(center: {
							latlng: [@location[:decimal_latitude], @location[:decimal_longitude]],
							zoom: 12 },
						markers: [
							{ latlng: [@location[:decimal_latitude], @location[:decimal_longitude]] }
						]) %>
	</div>
<% end %>